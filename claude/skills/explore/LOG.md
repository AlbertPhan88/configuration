# Explore skill — feedback log

Append-only. One line per signal. Status: OPEN → RESOLVED(vX.Y) | WONTFIX.
Types: signal (gap / re-ask — menu-learning data, not a defect) | override | format | pace.
Scope: entries may drive micro-Δ only (menus, wording, format, pacing). Methodology Δ → PROPOSALS.md via /explore-review.

| Date | Type | Observation | Status |
|---|---|---|---|
| 2026-08-16 | format | Mermaid fences shown as raw text in user's terminal — inline fence useless as primary visual | RESOLVED(v0.2) |
| 2026-08-16 | format | SendUserFile display:render targets desktop side panel; user is CLI-only → switched to inline ASCII box art | RESOLVED(v0.3) |
| 2026-08-17 | pace | GPU trial: term clarifications introduced new unknown terms → recursive explosion, user unsure whether to drill or park | RESOLVED(v0.7) |
